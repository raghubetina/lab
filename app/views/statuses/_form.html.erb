<%= form_for @status, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :step_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :step_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :imac, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :imac, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :progress, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :progress, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                statuses_path, :class => 'btn' %>
  </div>
<% end %>
